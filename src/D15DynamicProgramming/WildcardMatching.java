package D15DynamicProgramming;
//Given a text and a wildcard pattern, implement wildcard pattern matching algo
// that finds if wildcard pattern is matched with text, The matching should cover the entire
// text (not partial text). The wilddcard pattern can include the char ? and *
// ? - matches any single char
// * matches any sequence of char(including empty sequence

//Text = "baaabab
//pattern = "*****ba*****ab"
//output = true

//Text = "baaabab
//pattern = "a*****ab"
//output = false
public class WildcardMatching {
}
